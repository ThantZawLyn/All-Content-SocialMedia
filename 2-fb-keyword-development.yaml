apiVersion: apps/v1
kind: Deployment
metadata:
  name: fb-keyword-app
  namespace: crawler-apps
spec:
  replicas: 3 # Adjust the number of replicas as needed
  selector:
    matchLabels:
      app: fb-keyword-app
  template:
    metadata:
      labels:
        app: fb-keyword-app
    spec:
      containers:
      - name: fb-keyword-app
        image: mglue/my-python-app:1.0
        ports:
        - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: my-python-app-service
spec:
  selector:
    app: fb-keyword-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer # Use "LoadBalancer" or "NodePort" as per your setup
